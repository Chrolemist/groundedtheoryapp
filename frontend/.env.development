# Development defaults
# Disable backend websockets locally; collaboration runs between browser tabs.
VITE_DISABLE_WS=true

# Optional: if you run the backend on a different origin than the frontend dev server.
# VITE_API_BASE=http://localhost:8000
